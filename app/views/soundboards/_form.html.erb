<%= form_for(@soundboard) do |f| %>
  <% if @soundboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@soundboard.errors.count, "error") %> prohibited this soundboard from being saved:</h2>

      <ul>
      <% @soundboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
